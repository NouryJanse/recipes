import styled from 'styled-components'

const StyledIcon = styled.div`
  display: flex;
  position: relative;

  svg {
    min-height: 32px;
    width: 32px;
    margin: 8px;
  }
`

export default StyledIcon
